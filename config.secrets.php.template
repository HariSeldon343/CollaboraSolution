<?php
/**
 * CollaboraNexio — Secrets Overrides (LOCAL / TUNNEL / PROD)
 *
 * Copy this file to `config.secrets.php` (DO NOT COMMIT IT) and put secrets here.
 * This file is loaded very early by `config.php` to support Cloudflare tunnel setups
 * (where the host may trigger PRODUCTION_MODE even on localhost).
 *
 * Rules:
 * - Only guarded define() statements (if (!defined(...)) define(...))
 * - No side effects: no random_bytes(), no mkdir(), no headers, no ini_set, etc.
 */

declare(strict_types=1);

// OpenAI (Planning wizard proposal)
if (!defined('OPENAI_API_KEY')) {
    define('OPENAI_API_KEY', ''); // put your key here (private)
}

// Optional overrides
// if (!defined('OPENAI_MODEL')) define('OPENAI_MODEL', 'gpt-5.2'); // or another available model (e.g. gpt-4o-mini)
// if (!defined('OPENAI_TIMEOUT_SECONDS')) define('OPENAI_TIMEOUT_SECONDS', 20);
// if (!defined('OPENAI_API_BASE')) define('OPENAI_API_BASE', 'https://api.openai.com');

// Perplexity (AI Hub / multi-provider)
if (!defined('PPLX_API_KEY')) {
    define('PPLX_API_KEY', ''); // put your key here (private)
}
// Optional overrides
// if (!defined('PPLX_API_BASE')) define('PPLX_API_BASE', 'https://api.perplexity.ai');
// if (!defined('PPLX_DEFAULT_MODEL')) define('PPLX_DEFAULT_MODEL', 'sonar');
